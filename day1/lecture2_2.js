// 2.ES2018
// 템플릿 문자열(백틱, `)

const a = 'hello';
const b = true;
const c = 3;


// const d = a + ' ' + b + ' ' + c ;
// 백틱(`)사용 시 같은 출력
const d = `${a} ${b} ${c}`;

// 응용
const e = `${a}의 값은 ${b}이고 ${c}는 몰라요`;
// 따옴표의 구분이 수월함
const g =  `안녕하세요 작은따옴표(')입니다`;

